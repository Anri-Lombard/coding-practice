#include <bits/stdc++.h>

#define Inc(a, b, c) for (int c=a; c<b; c++)
#define Dec(a, b, c) for (int c=b-1; c>-1; c--)
#define vi vector<int>
#define vl vector<long>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int num;
    cin >> num;

    int n, m, rb, cb, rd, cd;
    cin >> n >> m >> rb >> cb >> rd >> cd;

    // robot = b, cleaner = d
    // O(n^2)
    Inc(0, n, row){
        Inc(0,m, column){
            if (row)
        }
    }

    
}